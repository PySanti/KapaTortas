"use client"

import { Pedido } from "@/app/models/Pedido";

export default function MainPedido({ pedido }: { pedido?: Pedido }) {
    return (
        <div>
            <p>HOla</p>
        </div>
    )
}