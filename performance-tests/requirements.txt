# Dependencias para pruebas de carga y estrés

# Herramienta principal de pruebas de carga
locust==2.15.1

# Para descarga de imágenes de Cloudinary
requests==2.31.0

# Procesamiento asíncrono (opcional pero recomendado)
celery==5.3.4
redis==5.0.1

# Caché de Django con Redis (opcional)
django-redis==5.4.0

# Análisis de resultados
pandas==2.1.3
matplotlib==3.8.2

# Cliente de PostgreSQL para Python (para scripts de verificación)
psycopg2-binary==2.9.9

# Rate limiting (opcional)
django-ratelimit==4.1.0
